#include<iostream>
using namespace std;
int main()
{
    int n;
    cin>>n;
    string num=to_string(n);
    int l=num.length();
    bool found=true;
    for(int i=0;i<l;i++)
    {
       if(num[i]=='1' || num[i+1]=='1' && num[i+2]=='4' ||num[i])
    return 0;
}